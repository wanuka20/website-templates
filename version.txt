CURRENT VERSION: 0.5.3

VERSION HISTORY:
  - 2026-07-23 - v0.5.3: production quality, accessibility, and deployment readiness.
    - Added server-side lead validation coverage and a silent honeypot, with durable shared per-IP rate limiting deferred to the future database-backed path.
    - Remediated public-route accessibility issues, removed placeholder links, added legal placeholders, and restored the templates' bright primary colour identity.
    - Added production security headers, canonical metadata, robots, sitemap, browser icon, and image/cache documentation.
    - Made missing Google Sheets content explicit while retaining an intentional `#FALLBACK` escape hatch for local defaults.
    - Verified the Vercel preview's HTTPS, headers, ISR, optimized image caching, public routes, and one cleaned-up live lead submission for each template.

  - 2026-07-18 - v0.5.2: reliable lead submissions and verified lead-delivery recovery.
    - Moved all five reference Apps Script files into `GoogleAppsScript/` and made lead values text-safe, so phone numbers beginning with `+` are stored correctly instead of becoming formulas or `#ERROR!`.
    - Added the same-origin `/api/leads` proxy so the website only reports success after Apps Script returns a successful HTTP response with `{ "ok": true }`.
    - Preserved form values after failed submissions and added distinct handling for HTTP errors, network failures, malformed responses, and delayed responses.
    - Added a configurable 15-second contact-form timeout through `NEXT_PUBLIC_LEAD_SUBMISSION_TIMEOUT_MS`, with a separate timeout retry message and restored submit button.
    - Added unit and Chromium regression tests for success, HTTP 500, network failure, duplicate clicks, malformed responses, and timeout behavior.
    - Submitted and verified one labelled live lead per template, confirmed correct text phone values and exactly one row per Sheet, then deleted the five exact test rows and confirmed no markers remained.

  - 2026-07-18 - v0.5.1: flexible image hosting and complete Google Sheets-to-site content mappings.
    - Added `ALLOW_ANY_IMAGE_HOSTS=true` to allow any HTTPS image hostname through `next/image` when desired; the default remains the restricted `remotePatterns` allowlist when the variable is missing or not set to `true`.
    - Added reusable `BrandLogo` rendering with the existing template icon as a fallback, applied to desktop/mobile navbars and footers across all five templates.
    - Rendered all configured social links in every template footer.
    - Made Salon booking links open a configured HTTP(S) booking service, with a safe Contact fallback for blank, unsafe, and legacy `#booking` values.
    - Updated Real Estate property cards to use the Sheet type, currency, features, and every supplied image.
    - Added Tuition Sheet testimonials and achievement-icon rendering.
    - Made Gym and Restaurant About images, plus Salon opening-hours text, editable from Settings sheets.
    - Added `addMissingSettingsRows()` to the Gym, Restaurant, and Salon Apps Script files; it appends only missing Settings rows and does not clear existing entries.

  - 2026-07-17 - v0.5.0: performance, caching, image, motion, and production-readiness improvements.
    - Optimized site images with `next/image`, lazy-loaded below-the-fold images, and prioritized hero images.
    - Replaced invalid Unsplash URLs with Google Drive placeholders and moved fallback images to local `public/placeholder_images` assets.
    - Mirrored the complete 76-file placeholder collection locally and updated spreadsheet image cells with matching Drive links.
    - Added configurable Google Apps Script caching with `GOOGLE_SHEETS_REVALIDATE_SECONDS` and removed unnecessary dynamic rendering from template pages.
    - Added request timeouts and cached-content fallback behavior for unavailable Google Apps Script responses.
    - Removed the global transition rule, reduced Framer Motion usage, added reduced-motion support, and optimized navbar scroll handling.
    - Simplified expensive blur, shadow, and image-transform effects.
    - Added `npm run cache:clear` and `npm run dev:fresh` for local Next.js cache management.
    - Added Lighthouse, Chrome Performance, bundle-size, Vitest, Playwright, Axe, resilience, SEO, and production-probe testing workflows.
    - Audited all public routes and five template websites across desktop and mobile browsers, including live form submission and cleanup verification.
    - Added the detailed production-readiness findings to `reports/production-readiness/REPORT.md`.

  - 2026-06-27 - v0.4.0: all five templates have separate Apps Script files, sheet-driven content wiring, and per-template env vars.

  - 2026-06-26 - v0.3.0: admin sheets created for all templates.

  - 2026-06-26 - v0.2.0: gym admin sheet + Apps Script integration working.

  - 2026-06-26 - v0.1.0: static template site exists.
