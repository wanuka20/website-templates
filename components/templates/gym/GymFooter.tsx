import { Dumbbell } from "lucide-react";
import type { GymConfig } from "@/types";
import { BrandLogo } from "@/components/templates/BrandLogo";
import { SocialLinks } from "@/components/shared/SocialLinks";

export function GymFooter({ config }: { config: GymConfig }) {
  return <footer className="border-t border-white/10 bg-[#050505] py-12 text-white"><div className="container mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><div className="grid gap-10 md:grid-cols-3"><div><div className="flex items-center gap-2 text-lg font-black uppercase tracking-[-0.04em]"><BrandLogo src={config.logo} alt={config.name} size={24} className="h-6 w-6 object-contain" fallback={<Dumbbell className="h-5 w-5 text-red-500" />} />{config.name}</div><p className="mt-4 max-w-xs text-sm text-zinc-400">{config.tagline}</p><SocialLinks links={config.socialLinks} className="mt-5 flex items-center gap-3" linkClassName="text-zinc-500 transition-colors hover:text-red-500" /></div><div><p className="mb-4 text-[10px] font-bold uppercase tracking-[0.18em] text-red-500">Quick links</p><ul className="grid grid-cols-2 gap-x-5 gap-y-3 text-sm text-zinc-400">{["About", "Membership", "Trainers", "Classes", "Gallery", "Contact"].map((item) => <li key={item}><a href={`#${item.toLowerCase()}`} className="transition-colors hover:text-white">{item}</a></li>)}</ul></div><div><p className="mb-4 text-[10px] font-bold uppercase tracking-[0.18em] text-red-500">Contact</p><div className="space-y-2 text-sm text-zinc-400"><p>{config.phone}</p><p>{config.email}</p><p>{config.address}</p><p>{config.city}</p></div></div></div><div className="mt-10 border-t border-white/10 pt-6 text-xs text-zinc-400">© {new Date().getFullYear()} {config.name}. All rights reserved.</div></div></footer>;
}
